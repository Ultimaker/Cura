[general]
definition = ultimaker_factor4_plus
name = Fast
version = 4

[metadata]
material = generic_pva
quality_type = draft
setting_version = 27
type = quality
variant = BB 0.4
weight = -2

[values]
acceleration_support_bottom = 100
acceleration_support_interface = 1500
brim_replaces_support = False
build_volume_temperature = =40 if extruders_enabled_count > 1 else 35
default_material_bed_temperature = =0 if extruders_enabled_count > 1 else 60
initial_layer_line_width_factor = 150
prime_tower_max_bridging_distance = =line_width
prime_tower_min_volume = 15
skin_material_flow = =material_flow * 0.93
speed_print = 50
support_brim_minimum_hole_area = 3
support_brim_width = =20*support_line_width
support_infill_multiplier = =2 if support_pattern == 'lightning' else 1
support_infill_rate = =10 if support_structure == 'tree' else 80 if gradual_support_infill_steps != 0 else 15
support_interface_offset = 1
support_lightning_angle = 40
support_offset = 3
support_pattern = ='lightning' if support_structure == 'tree' else 'zigzag'
support_structure = tree
support_tree_angle = 45
support_tree_angle_slow = 40
support_tree_bp_diameter = 30
support_tree_branch_diameter_angle = 5
support_tree_max_diameter = 25
support_tree_tip_diameter = 3
support_tree_top_rate = 30
support_wall_count = =1 if (support_structure == 'tree' and support_pattern != 'lightning') else 0
support_xy_distance_overhang = 0.2
support_z_distance = 0

